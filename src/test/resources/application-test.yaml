greeting: wrong
nested.value: wrong
outside.value: outside-hello
sample:
  greeting: hello
  nested.value: nested-hello
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      jakarta.persistence.create-database-schemas: true
  datasource:
    hikari:
      initialization-fail-timeout: 100
